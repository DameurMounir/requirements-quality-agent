# Offline workflow fixture

`candidate-analysis.json` is generated by `scripts/build_fixture.py` from the
transparent rule baseline and bound to the exact source-pack digest.

Its purpose is to reproduce workflow, evidence, approval, and export behavior
without a network or API key. It is not independent model output and must never
be presented as AI accuracy evidence.
