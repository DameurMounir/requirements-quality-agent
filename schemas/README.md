# Generated JSON Schemas

These schemas are generated from the Pydantic models in
`src/requirements_quality_agent/domain/models.py`.

Regenerate them with:

```bash
uv run python scripts/export_schemas.py
```

CI regenerates the files and fails if the committed versions differ. Do not
edit a generated schema directly.

